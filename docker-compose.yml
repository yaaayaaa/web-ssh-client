services:
  web-ssh:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    volumes:
      - ssh-data:/app/data
      - ./public:/app/public:ro
    restart: unless-stopped

volumes:
  ssh-data:
